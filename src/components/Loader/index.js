import React from 'react';
import * as S from './style';

const Loader = () => {
  return (
    <S.LoaderElement>
      <S.LoaderText>Loading...</S.LoaderText>
    </S.LoaderElement>
  );
};

export default Loader;
